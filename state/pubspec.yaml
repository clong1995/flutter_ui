name: state
description: A new Flutter project.
version: 0.1.0
homepage: flutter_ui

environment:
  sdk: ^3.6.0
  flutter: ">=3.19.6"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
